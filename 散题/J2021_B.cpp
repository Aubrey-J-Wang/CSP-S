#include<iostream> //...
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
//  freopen("sort.in", "r", stdin);
//  freopen("sort.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	int a[n+1] = {-1};
	for(int &i : a) cin >> i;
	for(int i = 1; i <= q; i++)
	return 0;
}
